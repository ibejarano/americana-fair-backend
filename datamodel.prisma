type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
}

type Item {
  id: ID! @id
  title: String!
  price: Int!
  image: String!
  largeImage: String!
  description: String!
}